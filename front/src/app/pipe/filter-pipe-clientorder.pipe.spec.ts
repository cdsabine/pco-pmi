import { FilterPipeClientorderPipe } from './filter-pipe-clientorder.pipe';

describe('FilterPipeClientorderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeClientorderPipe();
    expect(pipe).toBeTruthy();
  });
});
