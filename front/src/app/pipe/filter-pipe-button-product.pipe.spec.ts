import { FilterPipeButtonProductPipe } from './filter-pipe-button-product.pipe';

describe('FilterPipeButtonProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeButtonProductPipe();
    expect(pipe).toBeTruthy();
  });
});
