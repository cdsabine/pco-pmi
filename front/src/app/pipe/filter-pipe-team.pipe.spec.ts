import { FilterPipeTeamPipe } from './filter-pipe-team.pipe';

describe('FilterPipeTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
