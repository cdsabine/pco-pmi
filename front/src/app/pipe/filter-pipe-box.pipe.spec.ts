import { FilterPipeBoxPipe } from './filter-pipe-box.pipe';

describe('FilterPipeBoxPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeBoxPipe();
    expect(pipe).toBeTruthy();
  });
});
