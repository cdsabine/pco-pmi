import { FilterPipeProductPipe } from './filter-pipe-product.pipe';

describe('FilterPipeProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeProductPipe();
    expect(pipe).toBeTruthy();
  });
});
